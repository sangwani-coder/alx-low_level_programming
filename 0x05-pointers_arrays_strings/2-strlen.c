#include "main.h"
#include <string.h>

/**
 * _strlen - find the len of a string
 * @s: string
 *
 * Return: Length of string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
