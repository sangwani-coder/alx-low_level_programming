#include <stdio.h>
/**
 *_islower - locate a character.
 *@s: string to be searched..
 *@c: character to search for.
 *
 * Return: poniter to first occurence of c.
 */
void _islower(void)
{

	/*while (*s != c && *s != '\0')
	{
		s++;
	}
		if (*s == c)
		{
			return (s);
		}
		else
		{
			return (NULL);
		}*/
}
