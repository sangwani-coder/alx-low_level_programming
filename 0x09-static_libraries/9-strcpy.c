#include <stdio.h>
/**
 *_strcpy - locate a character.
 *@s: string to be searched..
 *@c: character to search for.
 *
 * Return: poniter to first occurence of c.
 */
void _strcpy(void)
{

	/*while (*s != c && *s != '\0')
	{
		s++;
	}
		if (*s == c)
		{
			return (s);
		}
		else
		{
			return (NULL);
		}*/
}
